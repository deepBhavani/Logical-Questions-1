function calculateCirclePerimeter(radius) {
    return 2 * Math.PI * radius;
}

let circleRadius = 5;
let circlePerimeter = calculateCirclePerimeter(circleRadius);
console.log("Perimeter of the circle:", circlePerimeter);
